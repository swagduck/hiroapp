import { PrismaClient } from "@prisma/client"; const prisma = new PrismaClient(); async function main() { const users = await prisma.user.findMany({ where: { role: "CUSTOMER", customerCode: null } }); let count = 0; for (const user of users) { await prisma.user.update({ where: { id: user.id }, data: { customerCode: "KH" + Math.floor(10000 + Math.random() * 90000).toString() } }); count++; } console.log(`Backfilled ${count} users`); } main().catch(console.error).finally(() => prisma.$disconnect());
